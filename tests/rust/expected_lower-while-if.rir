RustIR v1
fn main(x:i32) -> i32 {
  while (lt (load x) (const 10))
  do
    if (eq (load x) (const 5))
    then
      ret (const 5)
    else
      ret (load x)
    end
  end
  ret (const 0)
}
