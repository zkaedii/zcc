[
  {
    "id": "CG-IR-003",
    "title": "stdout pointer corruption via stale binary sign extension",
    "cwe": "CWE-704",
    "cwe_name": "Incorrect Type Conversion or Cast",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "ir_to_x86.c",
    "symptom": "IR-compiled compiler segfaults on first fprintf call. stdout pointer truncated to 32-bit value by movslq instead of movq.",
    "root_cause": "Stale binary from previous build used movslq for 64-bit pointer load. Clean rebuild with correct instruction selection fixes it. The IR lowerer was emitting sign-extending loads for pointer-width operands.",
    "detection_method": "GDB: print $rdi showed truncated pointer value (upper 32 bits zeroed). Assembly inspection: movslq where movq required.",
    "fix_summary": "Full clean rebuild. Ensured pointer-width loads always use movq, not movslq.",
    "affected_functions": [
      "all fprintf/printf callers"
    ],
    "lines_changed": 0,
    "fix_type": "rebuild",
    "prime_energy_score": 9.2,
    "prime_phase": "bifurcation",
    "tags": [
      "pointer-truncation",
      "sign-extension",
      "stale-build",
      "ABI-violation"
    ],
    "before_pattern": "movslq <ptr_source>, %rdi",
    "after_pattern": "movq <ptr_source>, %rdi",
    "regression_test": "Any program calling printf/fprintf through IR-compiled code"
  },
  {
    "id": "CG-IR-004",
    "title": "Phantom callee-save push/pop in body_only mode",
    "cwe": "CWE-682",
    "cwe_name": "Incorrect Calculation",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "compiler_passes.c:ir_asm_emit_one_block",
    "symptom": "IR-compiled functions crash with corrupted stack. RSP shifted by 16 bytes due to duplicate callee-save push/pop.",
    "root_cause": "ir_asm_emit_one_block emitted pushq %rbx; pushq %r12 at function entry even though the AST prologue already saved callee-saved registers via movq to stack slots. The double-save shifted RSP by 16, misaligning all subsequent stack references.",
    "detection_method": "Assembly diff between AST and IR output showed extra push/pop instructions. GDB showed RSP 16 bytes lower than expected at function body entry.",
    "fix_summary": "Added body_only flag to IRAsmCtx struct. Guarded push/pop emission with !ctx->body_only check.",
    "affected_functions": [
      "all IR-compiled functions"
    ],
    "lines_changed": 8,
    "fix_type": "guard_condition",
    "prime_energy_score": 8.7,
    "prime_phase": "chaotic",
    "tags": [
      "stack-corruption",
      "callee-saved",
      "hybrid-frame",
      "RSP-misalignment"
    ],
    "before_pattern": "pushq %rbx\npushq %r12\n... (body) ...\npopq %r12\npopq %rbx",
    "after_pattern": "/* push/pop skipped in body_only mode \u2014 AST prologue handles callee saves */",
    "regression_test": "Any multi-function program compiled through IR"
  },
  {
    "id": "CG-IR-005",
    "title": "PHI liveness back-edge inversion, CONDBR unconditional copies, serial PHI lost-copy",
    "cwe": "CWE-682",
    "cwe_name": "Incorrect Calculation",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "compiler_passes.c:ir_asm_emit_phi_edge_copy",
    "symptom": "Functions with loops crash or produce wrong results. Variables swapped in for-loop \u2014 e.g., loop counter used as base address, pointer used as counter.",
    "root_cause": "Three interrelated bugs in PHI resolution: (1) Liveness analysis inverted back-edge direction, marking loop-carried variables as dead. (2) CONDBR emitted PHI copies on both branches unconditionally instead of only on the taken edge. (3) Serial PHI copy used rax as sole intermediate \u2014 when PHI_A writes to a slot that PHI_B reads, the serial copy corrupts the source value (classic lost-copy problem in SSA destruction).",
    "detection_method": "GDB on cc_alloc crash: r12=0xD8 (216 = size parameter) used as base address for cp[i]=0. Assembly showed mov %r12,%rax where r12 held size instead of calloc'd pointer. Same pattern in main(): argv/i swap.",
    "fix_summary": "Fixed liveness back-edge direction. Made CONDBR PHI copies conditional on edge taken. Implemented parallel copy via temporary spill slots for multi-PHI blocks.",
    "affected_functions": [
      "cc_alloc",
      "main",
      "all functions with loops carrying 2+ variables"
    ],
    "lines_changed": 45,
    "fix_type": "algorithm_fix",
    "prime_energy_score": 9.8,
    "prime_phase": "bifurcation_critical",
    "tags": [
      "PHI-resolution",
      "SSA-destruction",
      "lost-copy",
      "liveness",
      "loop-carried-variables"
    ],
    "before_pattern": "ir_asm_load_to_rax(ctx, phi->phi[p].reg);\nir_asm_store_rax_to(ctx, phi->dst);\n/* next PHI may read from phi->dst \u2014 gets rax (wrong value) */",
    "after_pattern": "/* Parallel copy: spill all PHI sources first, then store to destinations */",
    "regression_test": "for (i=0; i<10; i++) sum += i; \u2014 must return 45, not hang"
  },
  {
    "id": "CG-IR-006",
    "title": "Stack frame too small for IR spill slots",
    "cwe": "CWE-121",
    "cwe_name": "Stack-based Buffer Overflow",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "compiler_passes.c:zcc_run_passes_emit_body_pgo",
    "symptom": "Segfault in IR-compiled functions accessing deep spill slots. IR allocates slots up to -1376(%rbp) but AST frame only reserved 912 bytes.",
    "root_cause": "IR spill slot formula ir_asm_slot(r) = -8*(r+2) for ~170 virtual registers requires up to 1376 bytes below rbp. The AST prologue only emitted subq $912, %rsp. IR body writes past the allocated frame into unmapped or other function's stack.",
    "detection_method": "GDB showed segfault on movq to -1200(%rbp) when frame was only 912 bytes. Calculation: max_vreg * 8 + overhead > allocated frame.",
    "fix_summary": "Added pre-scan phase before IR emission: scan all instructions for max register ID, alloca count, and alloca byte sizes. Emit subq $ir_extra, %rsp with exact computed depth.",
    "affected_functions": [
      "all IR-compiled functions with >100 virtual registers"
    ],
    "lines_changed": 20,
    "fix_type": "pre_scan",
    "prime_energy_score": 8.5,
    "prime_phase": "chaotic",
    "tags": [
      "stack-overflow",
      "frame-size",
      "spill-slots",
      "hybrid-frame"
    ],
    "before_pattern": "subq $912, %rsp  /* AST frame \u2014 too small for IR */",
    "after_pattern": "subq $912, %rsp  /* AST frame */\nsubq $1504, %rsp  /* IR extension: pre-scanned */",
    "regression_test": "Compile function with 150+ local expressions through IR"
  },
  {
    "id": "CG-IR-007",
    "title": "OP_CALL alignment counted phantom callee-save pushes",
    "cwe": "CWE-682",
    "cwe_name": "Incorrect Calculation",
    "severity": "high",
    "domain": "compiler_codegen",
    "component": "compiler_passes.c:OP_CALL handler",
    "symptom": "Function calls in IR-compiled code segfault or produce wrong results due to stack misalignment at call sites.",
    "root_cause": "The OP_CALL handler computed stack alignment using n_csave = __builtin_popcount(ctx->used_callee_saved_mask), counting callee-saved registers as stack pushes. In body_only mode, the AST prologue saves these with movq (no RSP change), so the alignment calculation was wrong by n_csave * 8 bytes.",
    "detection_method": "Assembly inspection showed unnecessary alignment subq before call instructions. Stack pointer tracking through function body revealed cumulative misalignment.",
    "fix_summary": "Set n_csave = ctx->body_only ? 0 : __builtin_popcount(ctx->used_callee_saved_mask) in the OP_CALL handler.",
    "affected_functions": [
      "all IR-compiled functions that call other functions"
    ],
    "lines_changed": 2,
    "fix_type": "guard_condition",
    "prime_energy_score": 7.5,
    "prime_phase": "wandering",
    "tags": [
      "call-alignment",
      "SysV-ABI",
      "RSP-alignment",
      "16-byte-boundary"
    ],
    "before_pattern": "int n_csave = __builtin_popcount(ctx->used_callee_saved_mask);",
    "after_pattern": "int n_csave = ctx->body_only ? 0 : __builtin_popcount(ctx->used_callee_saved_mask);",
    "regression_test": "Function calling printf with 6+ arguments"
  },
  {
    "id": "CG-IR-008",
    "title": "AST/IR stack slot collision \u2014 parameter overwrite",
    "cwe": "CWE-787",
    "cwe_name": "Out-of-bounds Write",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "compiler_passes.c:ir_asm_vreg_location",
    "symptom": "First parameter (argc) overwritten by IR spill. GDB shows rdi = 0x2a00007fffffffe8: 42 (test return value) smashed into upper bytes of argv pointer.",
    "root_cause": "AST stores argc at -8(%rbp) and argv at -16(%rbp). IR spill formula ir_asm_slot(0) = -8*(0+2) = -16(%rbp) \u2014 same offset as argv. IR store of virtual register 0 overwrites argv parameter.",
    "detection_method": "GDB: rdi contained 0x2a00007fffffffe8 \u2014 0x2a (42) in upper 32 bits merged with valid stack address. Traced to movl %ecx, (%rax) writing 4 bytes to the 8-byte argv slot.",
    "fix_summary": "Added slot_base field to IRAsmCtx. In body_only mode, slot_base = -stack_size. Changed ir_asm_vreg_location to use ctx->slot_base - 8*(r+1), placing IR slots below the AST frame.",
    "affected_functions": [
      "all IR-compiled functions with parameters"
    ],
    "lines_changed": 10,
    "fix_type": "offset_relocation",
    "prime_energy_score": 9.5,
    "prime_phase": "bifurcation",
    "tags": [
      "slot-collision",
      "parameter-corruption",
      "hybrid-frame",
      "stack-layout"
    ],
    "before_pattern": "ir_asm_slot(r) = -8*(r+2)  /* collides with AST param at -16(%rbp) */",
    "after_pattern": "ir_asm_slot(r) = slot_base - 8*(r+1)  /* slot_base = -stack_size, below AST frame */",
    "regression_test": "int main(int argc, char **argv) { return argc; } \u2014 must return correct argc"
  },
  {
    "id": "CG-IR-009",
    "title": "Pre-scan frame depth computation for alloca",
    "cwe": "CWE-131",
    "cwe_name": "Incorrect Calculation of Buffer Size",
    "severity": "high",
    "domain": "compiler_codegen",
    "component": "compiler_passes.c:zcc_run_passes_emit_body_pgo",
    "symptom": "Functions with alloca (variable-length stack allocation) overflow the IR frame extension because alloca bytes were not counted in the pre-scan.",
    "root_cause": "CG-IR-006 fix pre-scanned max register ID but not alloca count or alloca byte sizes. Functions using stack arrays or large local buffers allocated via OP_ALLOCA exceeded the computed frame.",
    "detection_method": "Assembly review of pre-scan output showed ir_extra undercount for functions with OP_ALLOCA instructions.",
    "fix_summary": "Extended pre-scan to count OP_ALLOCA instructions and sum their imm (byte size) fields. Added alloca_bytes to ir_extra calculation: ir_extra = 8*(max_reg + n_alloca + 8) + alloca_bytes.",
    "affected_functions": [
      "functions with stack-allocated arrays or buffers"
    ],
    "lines_changed": 12,
    "fix_type": "pre_scan_extension",
    "prime_energy_score": 7.0,
    "prime_phase": "converging",
    "tags": [
      "alloca",
      "frame-size",
      "stack-overflow",
      "pre-scan"
    ],
    "before_pattern": "int ir_extra = 8 * ((int)max_reg + 8);",
    "after_pattern": "int ir_extra = 8 * ((int)max_reg + n_alloca + 8) + (int)alloca_bytes;",
    "regression_test": "Function with char buf[4096] compiled through IR"
  },
  {
    "id": "CG-IR-010",
    "title": "4-byte movl for pointer load/store truncation",
    "cwe": "CWE-704",
    "cwe_name": "Incorrect Type Conversion or Cast",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "compiler_passes.c:OP_LOAD/OP_STORE handlers",
    "symptom": "Pointer values truncated to 32 bits. 64-bit pointer stored with movl (4 bytes), upper 32 bits left as stale data. Subsequent 8-byte load retrieves corrupted pointer.",
    "root_cause": "IR bridge marks some pointer operations with imm=4 (member_size for int fields). The OP_LOAD and OP_STORE handlers used imm to select movl vs movq. For pointer-to-struct-member accesses where the member is int-sized, this produced movl for what should be a full-width pointer operation.",
    "detection_method": "grep -c movl.*(%rax) in IR output vs AST output. AST had 0 movl derefs, IR had 47. GDB showed pointers with garbage upper 32 bits.",
    "fix_summary": "Changed OP_LOAD and OP_STORE to always use movq regardless of imm field. On x86-64, all register-to-memory transfers are 8 bytes through 64-bit registers.",
    "affected_functions": [
      "all IR-compiled functions with pointer load/store"
    ],
    "lines_changed": 4,
    "fix_type": "instruction_selection",
    "prime_energy_score": 8.8,
    "prime_phase": "chaotic",
    "tags": [
      "pointer-truncation",
      "movl-vs-movq",
      "width-mismatch",
      "LP64-ABI"
    ],
    "before_pattern": "if (ins->imm == 4) fprintf(f, \"movl %%ecx, (%%rax)\\n\");\nelse fprintf(f, \"movq %%rcx, (%%rax)\\n\");",
    "after_pattern": "fprintf(f, \"movq %%rcx, (%%rax)\\n\");  /* always 8-byte on x86-64 */",
    "regression_test": "Struct with int member accessed through pointer \u2014 must not truncate"
  },
  {
    "id": "CG-IR-011",
    "title": "Callee-saved register mismatch between AST and IR",
    "cwe": "CWE-682",
    "cwe_name": "Incorrect Calculation",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "part4.c:codegen_func + compiler_passes.c",
    "symptom": "IR-compiled functions corrupt callee-saved registers. The AST prologue saves registers predicted by allocate_registers(), but the IR linear scan uses different registers. Caller's rbx/r12/r13/r14/r15 get clobbered.",
    "root_cause": "The AST register allocator (allocate_registers) predicts which callee-saved regs are needed based on AST liveness analysis. The IR backend's linear scan allocator makes independent decisions. The AST prologue only saves the AST-predicted set, but the IR body uses registers outside that set.",
    "detection_method": "Assembly diff showed IR body using r14 but prologue only saving rbx and r12. Caller's r14 corrupted on return.",
    "fix_summary": "When IR is active, force all 5 callee-saved registers (rbx, r12, r13, r14, r15) to be saved in the prologue regardless of AST prediction. Wasteful but correct.",
    "affected_functions": [
      "all IR-compiled functions"
    ],
    "lines_changed": 6,
    "fix_type": "force_save_all",
    "prime_energy_score": 8.2,
    "prime_phase": "wandering",
    "tags": [
      "callee-saved",
      "register-mismatch",
      "ABI-violation",
      "hybrid-frame"
    ],
    "before_pattern": "for (i = 0; i < 5; i++) {\n    if (used_regs & (1 << i))\n        save_reg(i);  /* only AST-predicted regs */\n}",
    "after_pattern": "if (ir_active) {\n    save_all_callee_saved();  /* force all 5 */\n} else {\n    save_predicted_only();\n}",
    "regression_test": "Function with high register pressure calling other functions"
  },
  {
    "id": "CG-IR-012b",
    "title": "33 dead accessor stubs returning zero \u2014 structurally hollow AST",
    "cwe": "CWE-476",
    "cwe_name": "NULL Pointer Dereference",
    "severity": "critical",
    "domain": "compiler_bridge",
    "component": "compiler_passes.c:node_kind/node_lhs/node_rhs/etc",
    "symptom": "IR backend builds structurally complete but semantically empty AST. Every node_kind() returns 0, every node_lhs() returns NULL. Optimization passes operate on garbage, producing miscompiled code that crashes or loops infinitely.",
    "root_cause": "compiler_passes.c contained 33 static stub functions for AST accessors (node_kind, node_lhs, node_rhs, node_func_name, node_num_args, node_cond, node_body, etc.) that all returned 0/NULL/empty. These were placeholders from early development that were never replaced with real implementations. Since they were static, the linker couldn't resolve them to the real functions in zcc.c.",
    "detection_method": "Added fprintf debug to ir bridge: node_kind() returned 0 for every node. Traced to static stubs in compiler_passes.c. grep -c 'return 0' on accessor functions found all 33.",
    "fix_summary": "Replaced all 33 static stub definitions with extern declarations, allowing the linker to resolve them to the real implementations in zcc.c.",
    "affected_functions": [
      "node_kind",
      "node_lhs",
      "node_rhs",
      "node_cond",
      "node_body",
      "node_init",
      "node_inc",
      "node_func_name",
      "node_num_args",
      "node_arg",
      "node_cases",
      "node_num_cases",
      "node_default_case",
      "node_case_val",
      "node_case_body",
      "node_member_offset",
      "node_member_size",
      "node_line_no",
      "node_is_global",
      "node_is_array",
      "node_is_func",
      "node_int_val",
      "node_str_id",
      "node_name",
      "node_lhs_ptr_size",
      "node_rhs_ptr_size",
      "node_stmts",
      "node_num_stmts",
      "node_then_body",
      "node_else_body",
      "node_compound_op",
      "node_flit",
      "node_ptr_elem_size"
    ],
    "lines_changed": 66,
    "fix_type": "stub_to_extern",
    "prime_energy_score": 9.9,
    "prime_phase": "bifurcation_critical",
    "tags": [
      "hollow-stubs",
      "dead-code",
      "linker-resolution",
      "bridge-layer",
      "static-shadowing"
    ],
    "before_pattern": "static int node_kind(struct Node *n) { return 0; }\nstatic struct Node *node_lhs(struct Node *n) { return NULL; }",
    "after_pattern": "extern int node_kind(struct Node *n);\nextern struct Node *node_lhs(struct Node *n);",
    "regression_test": "Self-host: IR-compiled compiler must produce identical output to AST-compiled"
  },
  {
    "id": "CG-IR-013",
    "title": "ZND_CALL missing from zcc_node_from_stmt \u2014 statement-level calls empty",
    "cwe": "CWE-839",
    "cwe_name": "Numeric Range Comparison Without Minimum Check",
    "severity": "critical",
    "domain": "compiler_bridge",
    "component": "compiler_passes.c:zcc_node_from_stmt",
    "symptom": "Statement-level function calls (e.g., validate_node(...); fprintf(...);) produce empty call nodes. IR emits callq *%rax through uninitialized vreg 0. Crash: RIP=0x1.",
    "root_cause": "zcc_node_from_stmt had no case for ZND_CALL. Statement-level calls (expressions used as statements) fell through to default:break, producing a ZCCNode with kind=ZND_CALL but no func_name, no arguments. The IR lowerer emitted an indirect call through register 0 (uninitialized, contains garbage).",
    "detection_method": "GDB: crash at callq *%rax where rax=0x1. Backtrace showed the call originated from a validate_node() statement in codegen. Assembly showed no function name in the call sequence.",
    "fix_summary": "Added delegation to zcc_node_from_expr for ZND_CALL case in zcc_node_from_stmt. The expr handler already populates func_name and arguments correctly.",
    "affected_functions": [
      "all functions with statement-level calls (most functions)"
    ],
    "lines_changed": 3,
    "fix_type": "missing_case",
    "prime_energy_score": 9.0,
    "prime_phase": "bifurcation",
    "tags": [
      "missing-switch-case",
      "call-emission",
      "bridge-layer",
      "indirect-call",
      "RIP-corruption"
    ],
    "before_pattern": "case ZND_CALL:\n    /* falls through to default:break \u2014 empty node */\n    break;",
    "after_pattern": "case ZND_CALL:\n    return zcc_node_from_expr(n);  /* delegate to expr handler */",
    "regression_test": "Function containing statement-level printf() or validate_node() calls"
  },
  {
    "id": "CG-IR-014",
    "title": "ZND_ASSIGN missing from zcc_node_from_expr \u2014 for-loop increment empty",
    "cwe": "CWE-839",
    "cwe_name": "Numeric Range Comparison Without Minimum Check",
    "severity": "critical",
    "domain": "compiler_bridge",
    "component": "compiler_passes.c:zcc_node_from_expr",
    "symptom": "For-loop increment expression (i = i + 1) silently dropped. Loop runs forever because i never increments. IR latch block contains only a branch, no assignment.",
    "root_cause": "zcc_node_from_expr had cases for ZND_COMPOUND_ASSIGN and all binary/unary operators, but no case for plain ZND_ASSIGN. Assignment expressions used in for-loop increments (which are expressions, not statements) fell through to default:break. The ZCCNode was created with kind=ZND_ASSIGN but lhs=NULL and rhs=NULL. The lowerer's ZND_ASSIGN handler returned 0 immediately on null lhs.",
    "detection_method": "Test suite: for_loop_sum test timed out (infinite loop). Added DIAG_INC debug print: node->inc existed (kind=4=ZND_ASSIGN) but lhs and rhs were both NULL. Traced to missing case in zcc_node_from_expr.",
    "fix_summary": "Added ZND_ASSIGN case to zcc_node_from_expr with z->lhs = zcc_node_from_expr(node_lhs(n)) and z->rhs = zcc_node_from_expr(node_rhs(n)). Same pattern as the stmt handler at line 3083.",
    "affected_functions": [
      "all functions with for-loop increments using plain assignment (i = i + 1)"
    ],
    "lines_changed": 4,
    "fix_type": "missing_case",
    "prime_energy_score": 8.5,
    "prime_phase": "wandering",
    "tags": [
      "missing-switch-case",
      "assignment-expression",
      "for-loop",
      "bridge-layer",
      "infinite-loop"
    ],
    "before_pattern": "/* zcc_node_from_expr: no ZND_ASSIGN case */\ncase ZND_COMPOUND_ASSIGN: { ... }\ncase ZND_CALL: { ... }\ndefault: break;",
    "after_pattern": "case ZND_ASSIGN:\n    z->lhs = zcc_node_from_expr(node_lhs(n));\n    z->rhs = zcc_node_from_expr(node_rhs(n));\n    break;\ncase ZND_COMPOUND_ASSIGN: { ... }",
    "regression_test": "for (i = 0; i < 10; i = i + 1) sum += i; \u2014 must return 45"
  },
  {
    "id": "CG-CURL-001",
    "title": "ND_CAST wrapper in function-pointer global initializers",
    "symptom": "Curl_cmalloc = (curl_malloc_callback)malloc emits .quad 0 instead of .quad malloc relocation. Runtime crash: indirect call through NULL function pointer in curl_url() -> Curl_ccalloc.",
    "root_cause": "emit_global_var() in part4.c checked gvar->initializer->kind directly, missing the ND_CAST wrapper that the parser creates for function pointer casts. The dispatch chain (ND_NUM, ND_ADDR, ND_VAR, ND_STR, ND_INIT_LIST) never matched ND_CAST, falling through to .zero emission.",
    "fix": "Strip ND_CAST wrappers before dispatch: `Node *init = gvar->initializer; while (init && init->kind == ND_CAST) init = init->lhs;` Then use `init` for all kind checks and value reads.",
    "fix_diff": "--- a/part4.c\n+++ b/part4.c\n@@ emit_global_var\n-    if (gvar->initializer->kind == ND_NUM) {\n+    Node *init = gvar->initializer;\n+    while (init && init->kind == ND_CAST) init = init->lhs;\n+    if (init && init->kind == ND_NUM) {",
    "file": "part4.c",
    "function": "emit_global_var",
    "line_range": "3009-3013",
    "commit": "41389d6",
    "codebase": "libcurl-8.7.1",
    "category": "global_initializer",
    "severity": "runtime_crash",
    "discovery_method": "GDB backtrace: call *%r10 with r10=0x0, Curl_ccalloc in .data section all zeros",
    "verification": "objdump -r -j .data easy.o shows R_X86_64_64 malloc/free/calloc/realloc/strdup relocations",
    "related_bugs": [],
    "tags": [
      "function_pointer",
      "cast",
      "data_section",
      "relocation",
      "global_variable"
    ]
  },
  {
    "id": "CG-CURL-002",
    "title": "SysV ABI va_list \u2014 void* pointer-arithmetic instead of 24-byte struct",
    "symptom": "curl_easy_setopt(easy, CURLOPT_URL, \"http://example.com\") crashes with strlen(NULL) inside Curl_vsetopt. va_arg(param, char*) extracts NULL instead of the URL string. Also causes garbled IP address formatting (snprintf va_list path).",
    "root_cause": "zcc-libc/stdarg.h defined: typedef void* va_list; #define va_start(v,l) (v=(void*)((char*)&l + sizeof(l))). This pointer-arithmetic approach assumes variadic args are contiguous on the stack after the last named param. On x86-64 SysV ABI, the first 6 integer args are in registers (rdi,rsi,rdx,rcx,r8,r9), not on the stack. Additionally, zcc_compat.h redefined __builtin_va_list to void*, preventing the correct __builtin_va_start codegen (already implemented in part4.c) from being reached during GCC preprocessing.",
    "fix": "Rewrote zcc-libc/stdarg.h with proper SysV ABI 24-byte va_list struct: typedef struct { unsigned int gp_offset; unsigned int fp_offset; void *overflow_arg_area; void *reg_save_area; } __va_list_tag; typedef __va_list_tag va_list[1]; va_start uses __builtin_va_start (ZCC codegen sets gp_offset=N*8, fp_offset=48+M*16, overflow=16(%rbp), reg_save=save_base(%rbp)). va_arg macro reads from reg_save_area+gp_offset, advances gp_offset+=8. Removed __builtin_va_list=void* from zcc_compat.h.",
    "fix_diff": "--- a/zcc-libc/stdarg.h\n+++ b/zcc-libc/stdarg.h\n-typedef void* va_list;\n-#define va_start(v,l) (v=(void*)((char*)&l + sizeof(l)))\n-#define va_arg(v, l) (*(l*)((v=(void*)((char*)v + sizeof(l))) - sizeof(l)))\n+typedef struct { unsigned int gp_offset; unsigned int fp_offset; void *overflow_arg_area; void *reg_save_area; } __va_list_tag;\n+typedef __va_list_tag va_list[1];\n+#define va_start(v,l) __builtin_va_start(v, l)\n+#define va_arg(v, type) (*(type *)((char *)(v)[0].reg_save_area + ((v)[0].gp_offset += 8, (v)[0].gp_offset - 8)))",
    "file": "zcc-libc/stdarg.h, zcc-libc/zcc_compat.h",
    "function": "N/A (header macros + __builtin_va_start in codegen_call)",
    "line_range": "part4.c:2079-2098",
    "commit": "0f4864d",
    "codebase": "libcurl-8.7.1",
    "category": "abi_calling_convention",
    "severity": "runtime_crash",
    "discovery_method": "GDB: break Curl_vsetopt, info registers shows rdx=va_list_ptr but va_arg reads garbage from stack instead of register save area",
    "verification": "Standalone test: sum_args(3,10,20,30)=60, get_str(0,\"hello world\")=\"hello world\". curl_easy_setopt(URL)=0, curl_easy_strerror(0)=\"No error\"",
    "related_bugs": [
      "CG-DOOM-005"
    ],
    "tags": [
      "va_list",
      "variadic",
      "sysv_abi",
      "register_save_area",
      "x86_64",
      "calling_convention",
      "stdarg"
    ]
  },
  {
    "id": "CG-ARM-001",
    "title": "get_callee_reg() emits x86 register strings into ARM codegen path",
    "cwe": "CWE-704",
    "cwe_name": "Incorrect Type Conversion or Cast",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "part4.c:get_callee_reg",
    "symptom": "ARM output contained movq %rax, %r12 \u2014 invalid on Cortex-M0+. arm-none-eabi-gcc rejects assembly.",
    "root_cause": "get_callee_reg() returned x86 register name strings unconditionally. No backend_ops guard existed for register string selection.",
    "detection_method": "grep -E 'movq|subq|rax|rbx' hello_zcc.s \u2014 x86 instructions found in ARM output",
    "fix_summary": "Added backend_ops guard: ARM path emits mov r0, r4 style via thumb_regs[] array instead of x86 names.",
    "affected_functions": [
      "get_callee_reg",
      "codegen_expr"
    ],
    "lines_changed": 40,
    "fix_type": "backend_guard",
    "prime_energy_score": 8.2,
    "prime_phase": "bifurcation",
    "tags": [
      "ARM",
      "register-allocation",
      "cross-compilation",
      "x86-leakage"
    ],
    "before_pattern": "fprintf(cc->out, \"    movq %s, %%rax\\n\", reg);",
    "after_pattern": "if (backend_ops) fprintf(cc->out, \"    mov r0, %s\\n\", reg); else fprintf(cc->out, \"    movq %s, %%rax\\n\", reg);",
    "regression_test": "zcc hello.c -target thumbv6m-none-eabi && grep -c 'movq' hello.s == 0",
    "target": "thumbv6m-none-eabi",
    "milestone": "ARM-M2"
  },
  {
    "id": "CG-ARM-002",
    "title": "global __asm__(\"...\") blocks crash parser",
    "cwe": "CWE-228",
    "cwe_name": "Improper Handling of Syntactically Invalid Structure",
    "severity": "critical",
    "domain": "compiler_parser",
    "component": "part3.c:parse_program",
    "symptom": "\"expected ',' or ')' after parameter\" on hello.c:6",
    "root_cause": "ZCC did not understand top-level inline __asm__ syntax, leading to parse failures on required standard library or custom vector stubs.",
    "detection_method": "Parser crash on hello.c testing",
    "fix_summary": "parse_program() intercepts global __asm__, parsing the string and emitting raw payload to cc->out.",
    "affected_functions": [
      "parse_program"
    ],
    "lines_changed": 15,
    "fix_type": "syntax_support",
    "prime_energy_score": 7.5,
    "prime_phase": "bifurcation",
    "tags": [
      "ARM",
      "parser",
      "boot-vectors",
      "assembly"
    ],
    "before_pattern": "/* no match for __asm__ */",
    "after_pattern": "if (match(\"__asm__\")) { consume_asm_string(); }",
    "regression_test": "compile boot vectors containing global asm without parser errors",
    "target": "thumbv6m-none-eabi",
    "milestone": "ARM-M2"
  },
  {
    "id": "CG-ARM-003",
    "title": "ND_SUB emits subq into ARM path",
    "cwe": "CWE-704",
    "cwe_name": "Incorrect Type Conversion or Cast",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "part4.c:codegen_expr ND_SUB case",
    "symptom": "subq %r11, %rax in ARM output \u2014 illegal on M0+",
    "root_cause": "ND_SUB branch emitted raw x86 format unconditionally, ignoring backend_ops interface abstraction.",
    "detection_method": "arm-none-eabi-gcc assembler rejection",
    "fix_summary": "backend_ops guard routes to subs r0, r0, r1 instead of subq.",
    "affected_functions": [
      "codegen_expr"
    ],
    "lines_changed": 5,
    "fix_type": "backend_guard",
    "prime_energy_score": 7.8,
    "prime_phase": "bifurcation",
    "tags": [
      "ARM",
      "arithmetic",
      "x86-leakage"
    ],
    "before_pattern": "fprintf(cc->out, \"    subq %%r11, %%rax\\n\");",
    "after_pattern": "if (backend_ops) backend_ops->emit_binary_op(cc, ND_SUB); else fprintf(cc->out, \"    subq %%r11, %%rax\\n\");",
    "regression_test": "build math.c on thumbv6m and assert zero subq presence",
    "target": "thumbv6m-none-eabi",
    "milestone": "ARM-M3"
  },
  {
    "id": "CG-ARM-004",
    "title": "ND_MUL emits imulq into ARM path",
    "cwe": "CWE-704",
    "cwe_name": "Incorrect Type Conversion or Cast",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "part4.c:codegen_expr ND_MUL case",
    "symptom": "imulq %r11, %rax in ARM output",
    "root_cause": "The backend_ops->emit_binary_op hook was not applied to ND_MUL.",
    "detection_method": "arm-none-eabi-gcc assembler rejection",
    "fix_summary": "backend_ops guard routes to muls r0, r1, r0.",
    "affected_functions": [
      "codegen_expr"
    ],
    "lines_changed": 5,
    "fix_type": "backend_guard",
    "prime_energy_score": 7.6,
    "prime_phase": "bifurcation",
    "tags": [
      "ARM",
      "arithmetic",
      "x86-leakage"
    ],
    "before_pattern": "fprintf(cc->out, \"    imulq %%r11, %%rax\\n\");",
    "after_pattern": "if (backend_ops) backend_ops->emit_binary_op(cc, ND_MUL); else fprintf(cc->out, \"    imulq %%r11, %%rax\\n\");",
    "regression_test": "compile stress_test and confirm absence of imulq",
    "target": "thumbv6m-none-eabi",
    "milestone": "ARM-M3"
  },
  {
    "id": "CG-ARM-005",
    "title": "Bitwise ops (AND/OR/XOR) emit x86 variants",
    "cwe": "CWE-704",
    "cwe_name": "Incorrect Type Conversion or Cast",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "part4.c:codegen_expr ND_BAND/BOR/BXOR",
    "symptom": "andq/orq/xorq in ARM output",
    "root_cause": "Bitwise handlers generated x86 instructions irrespective of selected target interface.",
    "detection_method": "arm-none-eabi-gcc assembler rejection",
    "fix_summary": "backend_ops guards route to ands/orrs/eors respectively via emit_binary_op.",
    "affected_functions": [
      "codegen_expr"
    ],
    "lines_changed": 15,
    "fix_type": "backend_guard",
    "prime_energy_score": 7.2,
    "prime_phase": "bifurcation",
    "tags": [
      "ARM",
      "bitwise",
      "x86-leakage"
    ],
    "before_pattern": "fprintf(cc->out, \"    andq %%r11, %%rax\\n\");",
    "after_pattern": "if (backend_ops) backend_ops->emit_binary_op(cc, ND_BAND); else fprintf(cc->out, \"    andq %%r11, %%rax\\n\");",
    "regression_test": "compile RP2040 MMIO assignments, zero x86 bitwise leaked",
    "target": "thumbv6m-none-eabi",
    "milestone": "ARM-M3"
  },
  {
    "id": "CG-ARM-006",
    "title": "Shift ops emit shlq/sarq/shrq into ARM path",
    "cwe": "CWE-704",
    "cwe_name": "Incorrect Type Conversion or Cast",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "part4.c:codegen_expr ND_SHL/SHR",
    "symptom": "movq %r11, %rcx; shlq %cl, %rax in ARM output",
    "root_cause": "Power of 2 shift optimizations bypassed the basic backend emitter hooks completely.",
    "detection_method": "assembler rejection during MMIO base offset loads",
    "fix_summary": "backend_ops guard routes to lsls/lsrs r0, r1 architecture blocks explicitly avoiding %cl.",
    "affected_functions": [
      "codegen_expr"
    ],
    "lines_changed": 12,
    "fix_type": "backend_guard",
    "prime_energy_score": 7.3,
    "prime_phase": "bifurcation",
    "tags": [
      "ARM",
      "shift",
      "x86-leakage",
      "optimization-leak"
    ],
    "before_pattern": "fprintf(cc->out, \"    shlq %%cl, %%rax\\n\");",
    "after_pattern": "if (backend_ops) backend_ops->emit_binary_op(cc, ND_SHL); else ...",
    "regression_test": "no shlq instructions discovered in stress_test execution",
    "target": "thumbv6m-none-eabi",
    "milestone": "ARM-M3"
  },
  {
    "id": "CG-ARM-007",
    "title": "ND_TERNARY conditional branches not guarded",
    "cwe": "CWE-704",
    "cwe_name": "Incorrect Type Conversion or Cast",
    "severity": "critical",
    "domain": "compiler_codegen",
    "component": "part4.c:codegen_expr ND_TERNARY",
    "symptom": "cmpq $0, %rax; je .L in ARM output \u2014 illegal on M0+",
    "root_cause": "The ternary operator handler missed label abstractions, dropping direct x86 string arrays in the assembly output.",
    "detection_method": "ld failure during branch linkage due to missing .L parameters and cmpq rejection",
    "fix_summary": "backend_ops guard routes to cmp r0, #0; beq .L and guarantees labels pass correctly through format parameters.",
    "affected_functions": [
      "codegen_expr"
    ],
    "lines_changed": 8,
    "fix_type": "backend_guard",
    "prime_energy_score": 8.0,
    "prime_phase": "bifurcation",
    "tags": [
      "ARM",
      "branching",
      "x86-leakage"
    ],
    "before_pattern": "fprintf(cc->out, \"    cmpq $0, %%rax\\n    je .L%d\\n\", lbl1);",
    "after_pattern": "if (backend_ops) fprintf(cc->out, \"    cmp r0, #0\\n    beq .L%d\\n\", lbl1);",
    "regression_test": "ternary operator tests parse into correct bne/beq instructions",
    "target": "thumbv6m-none-eabi",
    "milestone": "ARM-M4"
  },
  {
    "id": "BUG-015",
    "title": "parse_decl missing ensure_type for float initializers",
    "file": "part3.c",
    "symptom": "float x = 3.14f produces 0.0 at runtime",
    "root_cause": "Local variable declarations with single initializers bypassed ensure_type(). ND_FLIT (ty_double) assigned directly into float slot without ND_CAST, destroying mantissa across 8->4 byte boundary.",
    "fix": "Wrapped scalar initializers in parse_decl with ensure_type(cc, expr, vtype) to force ND_CAST insertion (cvtsd2ss)",
    "discovered_via": "float_test.c \u2014 sum/prod evaluating to 0.000000",
    "date": "2026-04-12",
    "severity": "critical",
    "category": "parser"
  },
  {
    "id": "BUG-016",
    "title": "Float binary ops hardcoded sd suffix ignoring single-precision",
    "file": "part4.c",
    "symptom": "Float arithmetic produces wrong results even with correct types",
    "root_cause": "ND_ADD/SUB/MUL/DIV float intercepts hardcoded \"sd\" (double) instructions regardless of node->type->size. Single-precision (4-byte) floats read 8 bytes from xmm registers.",
    "fix": "Dynamic width binding: char *sz = (node->type->size == 4) ? \"ss\" : \"sd\" applied to cvtsi2, add, sub, mul, div instructions",
    "discovered_via": "float_test.c \u2014 arithmetic on float variables",
    "date": "2026-04-12",
    "severity": "critical",
    "category": "codegen"
  },
  {
    "id": "BUG-017",
    "title": "Missing C default argument promotion for variadic float args",
    "file": "part3.c",
    "symptom": "printf(\"%f\", float_var) prints 0.000000 or garbage",
    "root_cause": "C standard requires float->double promotion for variadic arguments. ZCC passed raw 4-byte ss values in xmm0; callee read 8 bytes (sd), getting corrupted exponent.",
    "fix": "In parse_call(), after consuming named params, insert ND_CAST to ty_double for TY_FLOAT args and ND_CAST to ty_int for char/short args in variadic positions",
    "discovered_via": "float_test.c \u2014 printf with float variables without explicit (double) cast",
    "date": "2026-04-12",
    "severity": "critical",
    "category": "parser/ABI"
  },
  {
    "id": "BUG-018",
    "title": "cond_else_seen state leak across #endif boundaries",
    "file": "part0_pp.c",
    "symptom": "#elif chains silently fail — second #if block inherits stale cond_else_seen from previous block at same depth",
    "root_cause": "#endif handler decremented cond_depth without zeroing cond_else_seen[] and cond_satisfied[] at the popped depth. When a new #if pushed depth back to the same level, it inherited stale state. #elif saw cond_else_seen==1 and treated it as invalid '#elif after #else'.",
    "fix": "Zero cond_satisfied[depth] and cond_else_seen[depth] in #endif handler before decrementing. Also zero cond_else_seen on #if/#ifdef depth push.",
    "discovered_via": "pp_test.c test11 — #elif MODE == 2 producing test11_val=0 instead of 2",
    "date": "2026-04-12",
    "severity": "critical",
    "category": "preprocessor"
  },
  {
    "id": "BUG-019",
    "title": "#if expression evaluator lacks parenthesized sub-expressions and short-circuit &&",
    "file": "part0_pp.c",
    "symptom": "#if !defined(X) && (defined(A) || defined(B)) evaluates to true even when X is defined",
    "root_cause": "pp_eval_expr was a flat heuristic string matcher — no operator precedence, no parenthesis recursion, no short-circuit evaluation. !defined(X) returning false did not prevent (defined(A) || defined(B)) from being evaluated.",
    "fix": "Replaced with full recursive descent parser: pp_eval_or > pp_eval_and > pp_eval_compare > pp_eval_unary > pp_eval_primary. Handles parentheses, defined(), integer literals, comparisons (==, !=, >=, <=, >, <), and treats unknown identifiers as 0 per C standard.",
    "discovered_via": "stb_image.h — #if !defined(STBI_NO_SIMD) && (defined(STBI__X86_TARGET) || defined(STBI__X64_TARGET)) leaking SSE2/asm code through guards",
    "date": "2026-04-12",
    "severity": "critical",
    "category": "preprocessor"
  }
]