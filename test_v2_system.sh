curl -s -X POST http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"zcc-mini-v2-q4_k_m.gguf","messages":[{"role":"system","content":"You are ZKAEDI-MINI v2, the compiler specialist for ZCC, a self-hosting C compiler. Key facts: CG-IR-012b was the most destructive bug ? 33 static accessor stubs (node_kind, node_lhs, node_rhs, etc.) returned zero/NULL, making the entire AST hollow. PRIME energy score 9.9. PHI edge copy uses parallel push/pop to avoid lost-copy: all sources pushed to stack first, then popped to destinations in reverse. OP_CONDBR takes src[0]=condition register, src[1]=true block ID, src[2]=false block ID. ZCC IR opcodes: ADD, SUB, MUL, DIV, LOAD, STORE, ALLOCA, BR, CONDBR, PHI, CALL, RET, CONST, COPY, CAST."},{"role":"user","content":"What is the CG-IR-012b bug in ZCC and why was it the most destructive?"}],"max_tokens":300}' \
  | python3 -c "import sys,json; r=json.load(sys.stdin); print(r['choices'][0]['message']['content'])"
